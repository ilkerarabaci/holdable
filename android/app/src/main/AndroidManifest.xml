<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- AR-1 spike (ADR-003): ARCore needs the camera. AR is OPTIONAL so the app
         still installs + runs (viewer only) on non-AR devices. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera.ar" android:required="false"/>
    <!-- The model-conversion service (.blend/USD/STEP -> glb) is reached over
         the network — now a Cloud Run HTTPS endpoint, so no cleartext needed.
         (For local-Docker dev over plain http, re-add usesCleartextTraffic.) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:label="holdable"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- screenOrientation is locked to portrait (Faz B #9): the
             ThermionWidget re-applies its own setLensProjection on every
             texture reallocation, and a device rotation forces exactly such a
             realloc — which clobbers the orthographic / ultra-wide lens presets
             back to the default perspective. Pinning the orientation removes
             that realloc path so the chosen lens preset sticks. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- "Open with Holdable": receive a single .obj/.stl from another
                 app or the file manager. Handled by receive_sharing_intent. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="content"/>
                <data android:scheme="file"/>
                <data android:mimeType="application/octet-stream"/>
                <data android:mimeType="model/obj"/>
                <data android:mimeType="model/stl"/>
                <data android:mimeType="application/sla"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/octet-stream"/>
            </intent-filter>
        </activity>
        <!-- Native renderer (v0.2): flutter_scene needs the Impeller backend
             plus the Flutter GPU API. On Android both are off by default in
             this Flutter version, so opt in here via meta-data. The equivalent
             CLI flags only apply to `flutter run`, not to built APKs. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="true" />
        <meta-data
            android:name="io.flutter.embedding.android.EnableFlutterGPU"
            android:value="true" />
        <!-- AR-1 spike (ADR-003): ARCore as an OPTIONAL dependency — the app
             installs everywhere; AR features check availability at runtime. -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="optional" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
